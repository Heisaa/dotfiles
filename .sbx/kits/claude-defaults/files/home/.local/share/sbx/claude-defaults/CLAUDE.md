# Global defaults

## Subagents

Do not use subagents (the Agent tool, workflows, or any other parallel agent
spawning) unless I explicitly ask for them in the current request. Do the work
directly with the ordinary tools instead. A task that would merely benefit from
parallelism does not count as asking.
