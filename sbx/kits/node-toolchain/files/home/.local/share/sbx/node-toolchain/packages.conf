# Recreate the sandbox after changing this file; startup kits cannot be updated
# with sbx kit add. The launcher can override Node with SBX_NODE_VERSION.
# Major (26), exact version (26.8.1), or empty to keep the image's Node.
NODE_VERSION="${SBX_NODE_VERSION-26}"
APT_PACKAGES=""
NPM_GLOBAL_PACKAGES=""
